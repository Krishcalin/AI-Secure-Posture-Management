# Intentionally vulnerable Dockerfile for AI-SPM testing
FROM python:latest

ENV OPENAI_API_KEY=sk-proj-abc123def456ghi789jkl012mno345pqr678stu901

WORKDIR /app
COPY . .
RUN pip install -r requirements.txt

EXPOSE 8888
EXPOSE 8080

CMD ["python", "serve.py"]
